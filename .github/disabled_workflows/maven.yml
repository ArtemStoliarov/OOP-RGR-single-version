name: Deprecated Maven Workflow

# This workflow is deprecated. The project uses `build.yml` for CI.
# Kept for manual invocation only to avoid duplicate automatic runs.
on:
  workflow_dispatch: {}

jobs:
  deprecated-manual-run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Notice
        run: echo "This workflow is deprecated. Use .github/workflows/build.yml instead."

